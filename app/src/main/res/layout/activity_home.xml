<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bant"
   tools:context=".Principal">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Solicitar Ruta"
        android:textSize="20dp"
        android:padding="20dp"
        android:background="@drawable/boton"
        android:textColor="#fff"
        android:drawableTop="@drawable/logo"
        android:id="@+id/btnVoz"
        android:onClick="inicializarReconocimiento"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="74dp" />

<Button
    android:id="@+id/Acceso"
    android:onClick="pantChofer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true"
    android:layout_alignLeft="@+id/btnVoz"
    android:layout_alignStart="@+id/btnVoz"
   android:background="#000"
    android:textColor="#fff"
    android:text="Inicial Sesión" />

</RelativeLayout>